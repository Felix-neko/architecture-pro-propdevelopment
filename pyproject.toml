[project]
name = "architecture-pro-propdevelopment"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "kubernetes>=33.1.0",
    "tabulate[widechars]>=0.9.0",
    "colorama>=0.4.6",
]

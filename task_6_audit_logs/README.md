# Задание 6. Аудит активности пользователей и обнаружение инцидентов

## Как я запускался


```bash
minikube delete
bash setup_minikube.sh
kubectl logs -f kube-apiserver-minikube -n  kube-system | grep audit.k8s.io/v1  # для просмотра audit-логов
```

Хинт: Иногда `kubectl logs -f kube-apiserver-minikube -n  kube-system | grep audit.k8s.io/v1` лучше копипастить в консольку отдельно, в bash-скрипте оно почему-то выдавало лишние записи. 

И запускаем скрипт-имитатор-атаки:
```bash
bash simulate_incident.sh
```
Далее сохраняем свежие логи аудита, когда скрипт отработает (я сохранял в [`audit.log`](audit.log)) -- и напускаем на него скрипт фильтрации:
```bash

uv run python filter_dangerous_events.py audit.log &> audit.filtered.log
```

- [`audit_security_analysis.md`](audit_security_analysis.md): комментарии LLM Claude 4 Sonnet по сырым логам аудита на базе audit.log. 
  Здесь самое ценное -- это именно подтверждения, что все ключевые события из скрипта симуляции инцидента нашли своё отражение в сырых логах (с конкретными цитатами из логов, которые это подтверждают).
  - Первоначально я хотел просто скормить Claude скрипт симуляции инцидента и сырые логи аудита, попросив его найти в логах подтверждения всех событий из скрипта, оданко это просто так не получилось: без дополнительных подсказок он нашёл менее половину нужных событий в логах.
  - Пришлось действовать по-крестьяски: открыть логи в виде pandas-датафрейма, зайти в PyCharm-отладку, сделать View As Dataframe, открыв Excel-подобную табличку просмотра датафрейма -- и вручную искать там соответствующие названия подов, имена файлов, которые я хотел изменить, и другие имена.
  - Всё, что нужно, в сырых аудит-логах нашлось, и я дописал Claude дополнительные подсказки, что смотреть в логах. После этого он и смог сгенерировать красивый отчёт по обнаруженным событиям )) 
  
- [`filter_dangerous_events.py`](filter_dangerous_events.py) cкрипт фильтрации логов 
  - этот скрипт я сгенерировал в Claude 4 Sonnet на основе разбота сырых логов, а потом слегка доработал его руками, 
    чтобы он фильтровал именно по типам создаваемых ресурсов и не пытался дополнительнос смотреть на подозрительные имена ; ) 
- [`audit.filtered.log`](audit.filtered.log) -- отфильтрованный лог
- [`audit.filtered.commented.log`](audit.filtered.commented.log) -- отфильтрованный лог c комментариями Claude по каждому событию
  - оригинальные auditID в оригинальном логе находятся, вроде не галлюцинация = )


## Какие изменения я вносил в оригинальные скрипты и конфиги
### Политика аудита
Я честно пытался запустить `minikubе` с той политикой аудита, которую нам давали в задании, ничего в ней не меняя.
Но злоехидный `minikube` с нею упорно не желал заводиться. Я даже `\`-символы из политики убрал, не помогало.

Поэтому я достал Claude -- и нагенерировал себе новых политик аудита, тестируя их следующим образом.

UPD: уже потом, действуя методом проб и ошибок я понял, что нужно не только убрать \-символы, 
но и заменить `group: "*"` на `group: ""` (см. [`dump/audit-policy-4.yaml`](dump/audit-policy-4.yaml)), но это я уже потом понял, а 
логи снимал я с той политикой, которая сейчас лежит в [`audit-policy.yaml`](audit-policy.yaml)

### Скрипт-имитатор действий админа злоумышленника
Я честно пытался запустить оригинальный скрипт симуляции инцидента, который давали в задании, 
и запускал его на чистеньком `minikube` из под админа-по-умолчанию, однако значительная часть атак не отрабатывала.

В частности, у “злоумышленника” не получается:
- прочитать содержимое секретов;
- вызвать какой-либо скрипт в системном поде `coredns`;
- удалить политику аудита (хотя эту политика аудита я клал именно на это место, и специально заходил в minikube ssh и проверял, что она там есть).

Скрипты и логи:
- [`dump/incorrect_stuff/simulate_incident.sh`](dump/incorrect_stuff/simulate_incident.sh) -- оригинальный скрипт симуляции инцидента;
- [`dump/incorrect_stuff/simulate_incident_2.sh`](dump/incorrect_stuff/simulate_incident_2.sh) -- он же с построчными комментами
- [`dump/incorrect_stuff/simulate_incident_2.output.log`](dump/incorrect_stuff/simulate_incident_2.output.log) -- логи запуска (часть шагов там выдавала ошибку).

Скрипт симуляции инцидента я в итоге доработал, надеюсь, от оригинального замысла я отклонился не очень сильно:

[`simulate_incident.sh`](simulate_incident.sh) -- скрипт симулиции инцидента с моими доработками.


### P.S.
Да, верно ли я понимаю, что у всех остальных всё нормально отрабатывало с оригинальным скриптом-имитатором и с оригинальной политикой безопасности из задания?

